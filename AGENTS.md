# agent-tools

Monorepo for AI coding agent utilities. Main package: **mcpm** (MCP Manager).

## Quick Reference

```bash
cd packages/cli && yarn build    # Build
yarn run dev                     # Run locally
npx mcpm                         # Interactive mode
```
